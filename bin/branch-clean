#!/usr/bin/env ruby

# Reference in .gitconfig:
#   bc = "!~/bin/branch-clean"

branches = `git branch --no-color --list`.chomp.split("\n")
branches = branches.reject { |b| b.include?("master") }
`git branch -d #{branches}`

